{
  "name": "@oiano/shared",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc -b --force"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "rimraf": "^5.0.7",
    "typescript": "^5.4.5"
  }
}
